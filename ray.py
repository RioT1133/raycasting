class Ray:
    def __init__(self, x, y, z): # inclination is up-down (+-z), azimuth is left-right(+-y)
        self.x = x
        self.y = y
        self.z = z

    def cast(self, world):
        return
        # what the fuck